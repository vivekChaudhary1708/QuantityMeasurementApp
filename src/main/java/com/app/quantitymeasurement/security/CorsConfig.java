package com.app.quantitymeasurement.security;

// Intentionally left without Spring annotations.
// CORS is configured in SecurityConfig.
public final class CorsConfig {
    private CorsConfig() {
    }
}